import { StyleSheet, View } from 'react-native';

export default function MorningWrappedScreen() {
  return <View style={styles.container} />;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000000',
  },
});
